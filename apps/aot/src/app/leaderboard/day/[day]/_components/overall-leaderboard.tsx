//   // Assign points based on ranking
//   const points = submissions.map((submission, index) => ({
//     userId: submission.userId,
//     points: 100 - index, // #1 gets 100 points, #2 gets 99, ..., #100 gets 1
//   }));

//   return points;
// };

// export default function OverallLeaderboard() {
//   return <div></div>;
// }
